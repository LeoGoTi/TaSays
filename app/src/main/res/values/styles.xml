<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="trans" parent="AppTheme"></style>
    <!-- Text style "" -->
    <!-- Add this snippet to styles.xml. -->
    <style name="test">
        <item name="android:typeface">normal</item>
        <item name="android:textColor">#56737e</item>
        <item name="android:textSize">50sp</item>
        <item name="android:gravity">left</item>
    </style>
    <!-- Text style "" -->
    <!-- Add this snippet to styles.xml. -->
    <style name="test2">
        <item name="android:typeface">normal</item>
        <item name="android:textColor">#305671</item>
        <item name="android:textSize">50sp</item>
        <item name="android:gravity">left</item>
    </style>
    <!-- Text style "" -->
    <!-- Add this snippet to styles.xml. -->
    <style name="test3">
        <item name="android:typeface">normal</item>
        <item name="android:textColor">#352876</item>
        <item name="android:textSize">50sp</item>
        <item name="android:gravity">left</item>
    </style>
    <!-- Text style "" -->
    <!-- Add this snippet to styles.xml. -->
    <style name="test4">
        <item name="android:typeface">normal</item>
        <item name="android:textColor">#212721</item>
        <item name="android:textSize">36sp</item>
        <item name="android:gravity">center</item>
    </style>



</resources>
